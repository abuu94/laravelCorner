### Steps:
- Go to herd.laravel.com and download the Windows installer.
- Run the installer — Herd will automatically configure PHP, Composer, and Laravel globally.
- Verify installation:
  ```
  herd --version
  php -v
  composer -V
  ```
